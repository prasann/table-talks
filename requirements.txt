# Core dependencies
duckdb>=0.9.0
pandas>=2.0.0
pyarrow>=10.0.0  # Optional: for Parquet support and better performance
langchain>=0.1.0
langchain-community>=0.0.20
langchain-ollama>=0.1.0
ollama>=0.1.7
pyyaml>=6.0
click>=8.0.0
requests>=2.28.0

# New library-based dependencies (Phase 2 migration)
sqlalchemy>=2.0.0
great-expectations>=0.18.0
duckdb-engine>=0.9.0

# Formatting and display
rich>=13.0.0

# Development dependencies
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
